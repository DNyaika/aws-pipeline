version: 0.2

phases:
  install:
    commands:
      - npm install -g npm@11.15.0
      - npm install
  build:
    commands:
      - npm run build
      - npm run cdk -- synth
artifacts:
  base-directory: cdk.out
  files:
    - '**/*'